/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode:'class',// if we need to set mode into the react app then set the mode here
  theme: {
    extend: {},
  },
  plugins: [],
}

